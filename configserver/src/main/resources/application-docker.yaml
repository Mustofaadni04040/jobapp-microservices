spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Mustofaadni04040/application-config/

eureka:
  client:
    service-url:
      defaultZone: http://servicereg:8761/eureka/